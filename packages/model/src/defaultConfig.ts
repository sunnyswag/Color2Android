import { CodeType } from "./uiEvent";
import { UIStateData } from "./uiState";

export const defaultUIStateData: UIStateData = {
	codeType: CodeType.XmlResource,
	code: "// select nodes to get config code"
}